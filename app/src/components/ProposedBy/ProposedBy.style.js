import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapperProposedby: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  thumnailCreator: {
    marginRight: 10,
    width: 25,
    height: 25,
    borderRadius: 12.5,
  },
  proposedBy: {
    color: 'grey',
  }
});

export default styles;
