import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapperProposedby: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  thumnailCreator: {
    marginRight: 10,
  },
  proposedBy: {
    color: 'grey',
  }
});

export default styles;
