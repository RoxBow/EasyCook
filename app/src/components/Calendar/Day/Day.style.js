import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    alignItems: 'center',
    padding: 8
  },
  selectedDate: {
    backgroundColor: 'rgba(255, 172, 129, .2)',
    borderRadius: 10
  },
  text: {
    fontSize: 16
  }
});

export default styles;
