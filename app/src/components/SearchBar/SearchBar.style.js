import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    width: '80%',
    height: 40,
    alignSelf: 'center',
  },
  iconSearch: {
    padding: 5
  }
});

export default styles;
