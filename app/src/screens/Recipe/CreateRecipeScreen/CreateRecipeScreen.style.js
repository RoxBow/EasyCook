import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  picker: {
    borderRadius: 15,
    width: '100%',
    height: 0,
    backgroundColor: '#fff'
  },
  pickerText: {
    fontSize: 22
  }
});

export default styles;
