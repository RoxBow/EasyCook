import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#ddd',
    flex: 1,
    padding: 15
  },
  picker: {
    borderRadius: 15,
    width: '100%',
    height: 62,
    backgroundColor: '#fff'
  },
  pickerText: {
    fontSize: 22
  }
});

export default styles;
