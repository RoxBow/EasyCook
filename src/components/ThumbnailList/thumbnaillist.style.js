import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    padding: 12,
    backgroundColor: "#fff"
  },
  title: {
    fontWeight: "600",
  }
});

export default styles;


