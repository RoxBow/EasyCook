import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    backgroundColor: "#fff",
    width: "48%",
    padding: 10,
    marginBottom: 10,
    borderRadius: 4,
  }
});

export default styles;